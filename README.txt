This repository contains two projects that are related: contextmodel and datadictionary.
See the README files under each of those.

contextmodel is older, and will be replaced by the newer code at datadictionary.  It
was just moved into its own subdirectory for archival purposes.

The test directory here contains a few XML files that can be used for testing (either
project).
